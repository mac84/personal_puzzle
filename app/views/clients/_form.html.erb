<%= simple_form_for @client do |f| %>
  <%= f.input :name, :label => "Namn" %>
  <%= f.input :standard_rate, :label => "Standard timlön" %>
  <%= f.submit 'Spara uppdragsgivare', :class => %w[btn btn-success] %>
<% end %>