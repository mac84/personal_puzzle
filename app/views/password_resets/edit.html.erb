<%= simple_form_for @user, :url => password_reset_path(params[:id]) do |f| %>
  <%= f.input :password, :label => "Lösenord" %>
  <%= f.input :password_confirmation, :label => "Lösenordet igen" %>
  <%= f.submit 'Skapa nytt lösenord' %>
<% end %>